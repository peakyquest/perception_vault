<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>navsat_pose_localizer</name>
  <version>0.1.0</version>
  <description>
    ROS 2 node that converts GNSS data (NavSatFix) into localized odometry (Odometry)
    relative to a reference origin. Calculates position using UTM projection and estimates
    orientation (yaw) from movement direction.
  </description>
  <maintainer email="haroon300@hotmail.com">Haroon Rasheed</maintainer>
  <license>Apache-2.0</license>

  <!-- Build tool -->
  <buildtool_depend>ament_cmake</buildtool_depend>

  <!-- ROS 2 dependencies -->
  <depend>rclpy</depend>
  <depend>geographic_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>sensor_msgs</depend>

  <!-- Python dependencies (numpy and utm are typically installed via pip) -->
  <!-- Note: numpy and utm should be installed via pip or system package manager -->

  <!-- Testing dependencies -->
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
